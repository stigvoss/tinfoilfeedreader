@page "/feedcollections/{FeedCollectionId}"
@page "/feedcollections/{FeedCollectionId}/feed/{FeedId}"
@inherits TinfoilFeedReader.Client.Pages.CodeBehinds.FeedCollectionsComponent


<SideBar>
    <FeedMenu />
</SideBar>

<main>
    <h1>@Title</h1>

    @if (FeedEntries is object)
    {
        foreach (var entry in FeedEntries)
        {
            <p><a href="@entry.Link" target="_blank">@entry.Title</a> - <em>@entry.PublishDate.LocalDateTime</em></p>
        }
    }
</main>